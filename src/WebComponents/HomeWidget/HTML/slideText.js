const slideText = ({title, text }) =>
    (`
    <span class="widget-title">
        ${title}  
         <span class="widget-excerpt">
            ${text}
        </span>    
    </span>
`);

export default slideText;
